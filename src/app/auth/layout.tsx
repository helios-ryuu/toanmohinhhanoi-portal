import type { ReactNode } from "react";

export const metadata = {
    title: "Sign in — Toan Mo Hinh Ha Noi",
};

export default function AuthLayout({ children }: { children: ReactNode }) {
    return children;
}
